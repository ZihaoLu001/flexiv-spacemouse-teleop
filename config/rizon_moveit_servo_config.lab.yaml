# MoveIt Servo configuration for SpaceMouse teleoperation on the lab Rizon 4s.
#
# This is the SINGLE SOURCE OF TRUTH for the Servo settings this repo depends
# on. Install it into the flexiv_ros2 checkout with scripts/apply_servo_config.sh
# (which backs up and can restore the upstream file). Do NOT hand-edit the copy
# inside flexiv_ros2 — that is exactly how unrecorded safety drift happens.
#
# Deltas vs upstream flexiv_ros2 humble-v1.7 defaults, and why:
#   publish_period 0.034 -> 0.02       match the 50 Hz bridge for smooth motion
#   joint_topic /joint_states -> /flexiv_arm/joint_states
#                                      the aggregated /joint_states is 30 Hz,
#                                      too slow for Servo; use the arm-rate topic
#   ee_frame_name flange -> grav_tcp   control the GN01 open-finger TCP
#                                      (REQUIRES load_gripper:=true, otherwise
#                                      the URDF has no grav_tcp link and Servo
#                                      fails to start)
#   robot_link_command_frame flange -> base_link
#                                      teleop twists are interpreted in the base
#                                      frame; the sign_* values in
#                                      config/spacemouse_teleop.yaml are
#                                      calibrated for this frame
#   is_primary_planning_scene_monitor true -> false
#                                      move_group runs in the same stack and is
#                                      the primary planning scene owner
#
# check_collisions stays TRUE (upstream default). It was hand-disabled on the
# lab machine in April 2026 "temporarily for a gripper demo"; that disabled all
# self-collision deceleration and is not acceptable as a default. If collision
# checking falsely triggers with the gripper model, fix the SRDF collision
# matrix instead of turning the feature off.

use_gazebo: false # Whether the robot is started in a Gazebo simulation environment

## Properties of incoming commands
command_in_type: "unitless" # "unitless"> in the range [-1:1], as if from joystick. "speed_units"> cmds are in m/s and rad/s
scale:
  # Scale parameters are only used if command_in_type=="unitless"
  linear:  0.4  # Max linear velocity. Unit is [m/s]. Only used for Cartesian commands.
  rotational:  0.8 # Max angular velocity. Unit is [rad/s]. Only used for Cartesian commands.
  # Max joint angular/linear velocity. Only used for joint commands on joint_command_in_topic.
  joint: 0.5

## Properties of outgoing commands
publish_period: 0.02
low_latency_mode: false  # Set this to true to publish as soon as an incoming Twist command is received (publish_period is ignored)

# What type of topic does your robot driver expect?
# Currently supported are std_msgs/Float64MultiArray or trajectory_msgs/JointTrajectory
command_out_type: trajectory_msgs/JointTrajectory

# What to publish? Can save some bandwidth as most robots only require positions or velocities
publish_joint_positions: true
publish_joint_velocities: true
publish_joint_accelerations: false

## Plugins for smoothing outgoing commands
smoothing_filter_plugin_name: "online_signal_smoothing::ButterworthFilterPlugin"

# If is_primary_planning_scene_monitor is set to true, the Servo server's PlanningScene advertises the /get_planning_scene service,
# which other nodes can use as a source for information about the planning environment.
# NOTE: If a different node in your system is responsible for the "primary" planning scene instance (e.g. the MoveGroup node),
# then is_primary_planning_scene_monitor needs to be set to false.
is_primary_planning_scene_monitor: false

## MoveIt properties
move_group_name:  rizon_arm  # Often 'manipulator' or 'arm'
planning_frame: $(var robot_sn)_base_link # The MoveIt planning frame. Often 'base_link' or 'world'

## Other frames
ee_frame_name: $(var robot_sn)_grav_tcp  # Official GN01 open-finger TCP; requires load_gripper:=true
robot_link_command_frame:  $(var robot_sn)_base_link  # Teleop commands are issued in the base frame

## Stopping behaviour
incoming_command_timeout:  0.1  # Stop servoing if X seconds elapse without a new command
# If 0, republish commands forever even if the robot is stationary. Otherwise, specify num. to publish.
# Important because ROS may drop some messages and we need the robot to halt reliably.
num_outgoing_halt_msgs_to_publish: 4

## Configure handling of singularities and joint limits
lower_singularity_threshold:  17.0  # Start decelerating when the condition number hits this (close to singularity)
hard_stop_singularity_threshold: 30.0 # Stop when the condition number hits this
joint_limit_margin: 0.1 # added as a buffer to joint limits [radians]. If moving quickly, make this larger.
leaving_singularity_threshold_multiplier: 2.0 # Multiply the hard stop limit by this when leaving singularity (see https://github.com/ros-planning/moveit2/pull/620)

## Topic names
cartesian_command_in_topic: ~/delta_twist_cmds  # Topic for incoming Cartesian twist commands
joint_command_in_topic: ~/delta_joint_cmds # Topic for incoming joint angle commands
joint_topic: /flexiv_arm/joint_states
status_topic: ~/status # Publish status to this topic
command_out_topic: /rizon_arm_controller/joint_trajectory # Publish outgoing commands here

## Collision checking for the entire robot body
check_collisions: true # Check collisions?
collision_check_rate: 10.0 # [Hz] Collision-checking can easily bog down a CPU if done too often.
self_collision_proximity_threshold: 0.01 # Start decelerating when a self-collision is this far [m]
scene_collision_proximity_threshold: 0.02 # Start decelerating when a scene collision is this far [m]
